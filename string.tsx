//import lazy from 'next/dynamic'
//import * as normalizeUrl from './circumcise'

//const normalizeUrl = lazy(() => import('./circumcise').then((m) => m.normalizeUrl))
const x = "/images/heroku/trends.png"

/* var x = {
  id: 1,
  penis: "long",
};
 */
const xtra = x.split("/images/")
console.log(new Date())
